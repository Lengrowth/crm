# `CHAMP-C01-R1` — Company, product name, and branding

## Identity

- Package ID: `C01`
- Release identity: `CHAMP-C01-R1`
- Commercial classification: Included
- Lifecycle state: `in_development` (synthetic/value-free foundation only)
- Requirement owner: Champion acceptance authority, to be identified in `AGR-02`
- Acceptance authority: pending `AGR-02`
- Feature flag: no production branding change; standard settings remain unchanged

## Approved inputs required

- `ORG-01`: exact legal/operating name, address/contact, timezone, currency,
  fiscal-year start.
- `BRD-01`–`BRD-03`: final product name, approved assets/colors/domain, and
  Champion-versus-reseller branding decision.
- `GLV-08`: final-domain owner and timing at handover.

## Current implementation

The isolated `lenerp_core` branch `codex/champ-c01-r1` contains a value-free
`LenERP Branding Settings` Single DocType. It has no defaults, no Champion
records or assets, no permanent `lengrowth.com` dependency, and is restricted
to `System Manager`. It is a configuration boundary only; it does not apply
branding or perform domain cutover.

- Custom-app commit: `072f99d` (`lenerp_core` `0.2.0`)
- Static contract test: `python -m pytest -q tests` — 6 passed
- Package test: wheel contains the DocType JSON and Python module — PASS
- Frappe bench install/migrate: final protected staging run pending; the
  workflow deploys only the SHA256-pinned archive of the exact commit above;
  no production activation or Champion data is authorized

## Acceptance scenarios

- Clean install/migrate creates the settings boundary without requiring
  Champion values.
- Non-System-Manager users cannot read or write the settings document.
- Approved values can be entered once the Project Start decisions exist.
- Standard Company/System/Website settings remain unchanged until an approved
  release applies them.
- Generated links, print/PDF output, fallback assets, light/dark surfaces, and
  responsive behavior are tested with approved configuration.

## Recovery and evidence

- Code rollback: previous immutable `lenerp_core` package/release pointer.
- Data correction: clear or correct the additive settings document; no code
  rollback is treated as data correction.
- Required evidence: exact app commit/version, staging install/migrate,
  upstream cleanliness, permission tests, browser/print evidence, and written
  Champion acceptance.
- Current verdict: synthetic staging evidence captured; not accepted for
  Champion or production use.
