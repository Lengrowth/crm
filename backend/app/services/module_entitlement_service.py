class ModuleEntitlementService:
    """Placeholder service for module enablement and entitlement checks."""

    def list_enabled_modules(self, organization_id: str) -> list[str]:
        raise NotImplementedError("Module entitlements are introduced in a later phase.")
